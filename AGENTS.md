# DevChat Agent Guidelines

이 문서는 DevChat에서 사람과 AI가 함께 변경할 때 지켜야 할 저장소 규칙이다. 사용자의 직접 지시가 이 문서보다 우선한다.

## 작업 절차

- 기존 미커밋 변경과 관련 없는 파일을 수정하거나 되돌리지 않는다.
- 인증, WebSocket, 비동기 이벤트, 데이터베이스 마이그레이션, 배포, 인프라와 성능 주장은 고위험 변경으로 취급한다.
- 고위험 변경은 `조사 → 계획 → 사람 승인 → 구현 → 리뷰 → 검증` 순서로 진행한다.
- 구현자와 리뷰어 역할을 분리한다. 리뷰어는 명시적인 수정 요청 없이 코드를 변경하지 않는다.
- 구현 전에 실패하는 테스트로 기대 동작을 고정하고, 구현 후 관련 테스트와 회귀 테스트를 실행한다.
- AI 제안을 그대로 채택하지 않는다. 채택하거나 기각한 이유, 실행한 검증과 남은 리스크를 PR에 기록한다.

## 반드시 확인할 경계

- JWT 변경 시 Access/Refresh Token의 수명, Redis 상태, 회전 및 유예 처리와 로그아웃 무효화를 함께 확인한다.
- WebSocket/STOMP 변경 시 HTTP 인증과 다른 연결·재연결·구독 권한 경계를 확인한다.
- `@Async`와 `@TransactionalEventListener(AFTER_COMMIT)`은 커밋 이후 실패와 중복 실행 가능성을 전제로 설계한다. `convertAndSend` 성공을 브라우저 수신 보장으로 표현하지 않는다.
- 친구 관계, 커뮤니티 지원 알림과 DM 생성은 DB 저장과 실시간 전송의 트랜잭션 경계를 명시한다.
- Flyway 마이그레이션은 기존 데이터와 이전 배포 버전의 호환성을 확인하고, 적용된 migration 파일을 수정하지 않는다.
- 프런트엔드와 백엔드의 요청·응답 DTO, 오류 코드와 WebSocket destination 변경은 양쪽 계약을 함께 확인한다.
- Blue/Green 배포 변경은 외부 헬스체크, 전환 실패 조건과 롤백 절차를 함께 검증한다.
- TPS, 지연시간과 쿼리 개선 수치는 데이터 규모, 부하 조건, 실행 환경과 원본 결과를 함께 남긴다.

## AI 작업 기록

- Codex Logging Hook은 요청과 도구 실행의 추적을 돕는 관찰 장치이며 보안 경계나 권한 Guard가 아니다.
- 원본 프롬프트와 전체 도구 결과를 커밋하지 않는다. `ai/logs/*.jsonl`은 로컬에만 둔다.
- Hook 요약은 사실 기록의 초안이다. 사람이 검토한 뒤에만 PR 근거로 사용한다.
- 기록이 있다는 이유만으로 동일 코드 재현, AI 정확성 또는 개발 안정성 향상을 주장하지 않는다.
