# DevChat Agent Guidelines

이 문서는 DevChat에서 사람과 AI가 함께 변경할 때 지켜야 할 저장소 규칙이다. 사용자의 직접 지시가 이 문서보다 우선한다.

## 작업 절차

- 기존 미커밋 변경과 관련 없는 파일을 수정하거나 되돌리지 않는다.
- 인증, WebSocket, 비동기 이벤트, 데이터베이스 마이그레이션, 배포, 인프라와 성능 주장은 고위험 변경으로 취급한다.
- 고위험 변경은 `조사 → 계획 → 사람 승인 → 구현 → 리뷰 → 검증` 순서로 진행한다.
- 구현자와 리뷰어 역할을 분리한다. 리뷰어는 명시적인 수정 요청 없이 코드를 변경하지 않는다.
- 구현 전에 실패하는 테스트로 기대 동작을 고정하고, 구현 후 관련 테스트와 회귀 테스트를 실행한다. 새 테스트와 수정하는 테스트 메서드에는 테스트 의도를 한국어로 드러내는 JUnit `@DisplayName`을 작성한다.
- AI 제안을 그대로 채택하지 않는다. 채택하거나 기각한 이유, 실행한 검증과 남은 리스크를 PR에 기록한다.

## 변경 영역별 검토 규칙

다음 규칙은 해당 영역을 수정할 때만 적용한다.

- 적용된 Flyway migration은 수정하지 않고 새 migration을 추가한다.
- API DTO, 오류 코드와 WebSocket destination을 변경하면 프런트엔드·백엔드 계약과 하위 호환성을 함께 확인한다.
- TPS, 지연시간과 쿼리 개선 수치는 데이터 규모, 부하 조건, 실행 환경과 원본 결과를 함께 남긴다.

## AI 작업 기록

- Codex Logging Hook은 요청과 도구 실행의 추적을 돕는 관찰 장치이며 보안 경계나 권한 Guard가 아니다.
- 원본 프롬프트와 전체 도구 결과를 커밋하지 않는다. `ai/logs/*.jsonl`은 로컬에만 둔다.
- Stop Hook은 Markdown 요약을 자동 생성하지 않는다. 이전 `ai/summaries/` 파일은 로컬 보관 자료이며 Git에 커밋하지 않는다.
- 기록이 있다는 이유만으로 동일 코드 재현, AI 정확성 또는 개발 안정성 향상을 주장하지 않는다.

## 코드 변경 완료 기록과 PR

- 코드가 변경되고 관련 테스트가 통과하면, 최종 응답 전에 `docs/knowledge/changes/YYYY-MM-DD-<branch-or-topic>.md`에 작업당 하나의 사실 기반 지식 기록을 작성한다.
- 지식 기록은 `목적`, `변경 사항`, `영향 범위`, `검증`, `남은 리스크`를 포함한다. 계획·추측·원본 프롬프트·전체 도구 출력은 넣지 않는다.
- 새 지식 기록은 `ai/rag/corpus.json`에 `active`로 명시한다. RAG는 Git 추적 문서만 색인하므로 기록이 commit된 뒤 로컬 RAG index를 다시 생성해 검색에 반영한다. `docs/superpowers/plans/`, `docs/local/`, `draft`, `superseded` 문서는 RAG에 넣지 않는다.
- PR 본문은 `.github/PULL_REQUEST_TEMPLATE.md` 구조로 항상 작성한다. 현재 HEAD가 원격 현재 브랜치에 이미 push된 경우 `gh`로 draft PR을 생성하거나 갱신한다. 그렇지 않거나 `gh`를 사용할 수 없으면 Git 제외 `_workspace/`에 PR 본문 Markdown 초안을 만든다.
- PR 생성 전에는 `git remote -v`, head branch, base branch를 확인하고, 개인 저장소의 정확한 원격과 base branch를 사용자에게 명시한다. 다른 저장소에 만든 PR은 이동할 수 없으므로 닫고 올바른 저장소에 새 PR을 만든다.
- PR 본문 `작업 내용`에는 비자명한 변경마다 `문제와 목표`, `변경 사항`, `사용 방법 또는 동작 변화`, `주요 선택과 근거`, `영향 범위`를 포함한다. 단순 문서 수정이나 한 줄 버그 수정은 문제·변경·검증만 작성할 수 있다.
- 사용 방법은 사용자와 개발자·운영자의 책임을 구분한다. 최초 설치, 색인 갱신, 배포처럼 사용자가 직접 하지 않는 작업을 사용자 지시처럼 쓰지 않는다.
- 선택 근거에는 검토한 대안과 채택·기각 이유를 적고, 성능·검색 품질·호환성 주장은 측정 조건과 원본 결과를 함께 남긴다. 실행하지 않은 검증이나 확인하지 않은 동작을 PR에 쓰지 않는다.
- AI 작업 기록은 본문의 변경 사항을 반복하지 않고, AI 사용 범위·채택·기각 이유·실행한 검증·남은 리스크만 적는다.
- 위 문서화와 PR 본문 작성은 commit·push 권한을 포함하지 않는다. commit과 push는 사용자의 명시적 요청을 기다린다.

## AI PR 리뷰 규칙

- 리뷰 요약과 inline comment는 한국어로 작성한다.
- PR 목적, `AGENTS.md`, 변경 전후 계약과 관련 테스트를 확인한 뒤 실제 결함만 지적한다. 코드 스타일 취향, 범위 밖 리팩터링, 근거 없는 추측은 리뷰 항목으로 만들지 않는다.
- 각 항목에는 P0~P3 우선순위, 재현 조건, 사용자·데이터·운영 영향, 근거 파일·행, 최소 수정 방향을 포함한다.
- P0은 보안 침해·데이터 손실·서비스 중단 가능성, P1은 일반 흐름의 잘못된 동작 또는 명확한 회귀, P2는 제한된 조건의 결함·유지보수 위험, P3은 합병을 막지 않는 선택적 개선이다.
- 인증, 권한, WebSocket, 비동기 이벤트, DB migration, API 계약, 성능 변경은 관련 계약·하위 호환성·테스트 근거를 우선 확인한다.
- 테스트가 없다는 사실만으로 결함이라고 단정하지 않는다. 현재 변경으로 깨지는 동작을 설명할 수 있을 때만 지적한다.
- 확신하지 못한 항목은 결론 대신 확인이 필요한 조건을 적는다. 문제가 없으면 한국어로 검토 범위와 결과를 간결하게 남긴다.

## Git 작업과 커밋

- 사용자가 명시적으로 요청하지 않으면 commit, push, merge, rebase를 실행하지 않는다.
- 커밋 메시지는 `<type>: <한국어 작업 요약>` 형식을 사용한다. `type`은 `feat`, `fix`, `refactor`, `test`, `docs`, `perf`, `chore` 중 작업 성격에 맞는 값을 소문자로 쓴다.
- 하나의 커밋에는 하나의 논리적 변경만 담는다. 구현과 그 구현을 검증하는 테스트·문서가 하나의 변경을 완성한다면 함께 담을 수 있다.
- 커밋 전에는 staged diff와 관련 테스트 결과를 확인하고, 사용자가 요청하지 않은 파일은 stage하지 않는다.
- 짧은 커밋 메시지에는 `humanize-korean` 전체 워크플로우를 실행하지 않는다. 무슨 작업인지 바로 알 수 있도록 자연스러운 한국어로 짧게 쓴다.

## 외부 공개용 한국어 문서

- `humanize-korean`은 PR 제목·본문, Issue, 외부 공개 README·사용 안내, 이력서와 포트폴리오처럼 사람이 실제로 평가하거나 사용하는 최종 산출물에만 적용한다.
- 문서 길이는 적용 기준이 아니다. `docs/superpowers/plans`, 내부 설계 초안, `AGENTS.md`, 테스트 문서, 코드 주석, 작업 메모, Hook 요약 초안과 커밋 메시지에는 기본적으로 적용하지 않는다.
- 기본 제외 문서라도 사용자가 명시적으로 윤문을 요청하면 적용한다.
- 사실, 수치, 날짜, 코드 식별자, 명령어, API 계약, 파일 경로와 직접 인용은 바꾸지 않는다. 문체를 다듬는 과정에서 새로운 주장이나 근거를 추가하지 않는다.
- PR은 `.github/PULL_REQUEST_TEMPLATE.md` 구조를 유지한다. AI를 사용했다면 사용 범위, 채택·기각한 제안, 실행한 검증과 남은 리스크를 구체적으로 작성한다.
- `humanize-korean`을 사용할 수 없는 환경에서는 같은 원칙으로 직접 검토하며 문서 작업을 중단하지 않는다.
- `humanize-korean`의 `_workspace/` 산출물은 로컬 검토 자료로만 사용하고 Git에 커밋하지 않는다. 최상위 작업 폴더는 `YYYY-MM-DD-<topic>` 형식의 소문자 kebab-case 이름을 사용한다. 날짜와 숫자만으로 된 폴더명은 만들지 않는다.
- PR 본문 초안은 작업별 `_workspace/YYYY-MM-DD-<topic>-pr/final.md` 한 파일만 유지한다. 같은 작업의 후속 초안은 그 파일을 갱신하며, 루트에 복사본이나 이전 초안을 추가로 남기지 않는다.
