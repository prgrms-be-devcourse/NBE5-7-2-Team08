global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: devchat
    metrics_path: /metrics/devchat
    static_configs:
      - targets: ['gateway-nginx:8081']
        labels:
          app: devchat
          environment: production
